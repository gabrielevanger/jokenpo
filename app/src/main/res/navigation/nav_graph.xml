<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.myapplication.HomeFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_home">
        <action
            android:id="@+id/action_homeFragment_to_game_graph"
            app:destination="@id/game_graph" />
    </fragment>

    <navigation
        android:id="@+id/game_graph"
        app:startDestination="@id/playerFragment">

        <fragment
            android:id="@+id/playerFragment"
            android:name="com.example.myapplication.PlayerFragment"
            android:label="@string/player_label"
            tools:layout="@layout/fragment_player" />

        <fragment
            android:id="@+id/resultFragment"
            android:name="com.example.myapplication.ResultFragment"
            android:label="@string/result_label"
            tools:layout="@layout/fragment_result">
            <argument
                android:name="arg_current_play"
                app:argType="string"
                android:defaultValue="Pedra" />
        </fragment>
    </navigation>
</navigation>
