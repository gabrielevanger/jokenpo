<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Widget.Jokenpo.InputSpinner" parent="@android:style/Widget.Spinner">
        <item name="android:background">@drawable/bg_spinner</item>
        <item name="android:paddingStart">12dp</item>
        <item name="android:paddingEnd">12dp</item>
        <item name="android:minHeight">48dp</item>
    </style>

    <style name="TextAppearance.Jokenpo.InputSelected" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">#263238</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="TextAppearance.Jokenpo.InputDropdown" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">#263238</item>
        <item name="android:textSize">17sp</item>
    </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />
</resources>